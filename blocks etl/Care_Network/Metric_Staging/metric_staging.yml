version: 2

models:
  - name: stg_scorecard_spec_pfex_access
    tests:
      - dbt_chop_utils.is_primary_key:
          column_names: [primary_key]
  - name: stg_scorecard_spec_pfex_ltr
    tests:
      - dbt_chop_utils.is_primary_key:
          column_names: [primary_key]
  - name: stg_scorecard_spec_pfex_move_through_visit
    tests:
      - dbt_chop_utils.is_primary_key:
          column_names: [primary_key]
  - name: stg_scorecard_spec_pfex_communication
    tests:
      - dbt_chop_utils.is_primary_key:
          column_names: [primary_key]
  - name: stg_scorecard_spec_pfex_staff_worked_together
    tests:
      - dbt_chop_utils.is_primary_key:
          column_names: [primary_key]
