version: 2

models:
  - name: nursing_metric_pfex
    tests:
      - dbt_chop_utils.is_primary_key:
          column_names: [pfex_metric_key]
    columns:
      - name: pfex_metric_key
        description: PK